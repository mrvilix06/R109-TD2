$(function() { // Cette fonction est appelée une fois que la page est construite (Q4)
    setTimeout(function() { // fonction qui sera appelée après 5 secondes
        window.open("http://isis.unice.fr/"); // ouverture d'une nouvelle page sur la fenetre courante
    }, 5000); // 5000 ms = 5 secondes
});